defaults:
  - _self_
  - prob: setpacking
  - nsga2: default
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

from_pid: 0
to_pid: 100
n_processes: 1
split: train

# Seed controlling which instance files to load
inst_seed: 42

# Seeds controlling algorithm stochasticity across runs
run_seeds: [7, 8, 9, 10, 11]

hydra:
  output_subdir: null
  run:
    dir: .
